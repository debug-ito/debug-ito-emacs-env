# -*- mode: snippet -*-
# name: include header once
# key: head
# --
#ifndef ${1:`(replace-regexp-in-string "-" "_" (upcase (file-name-nondirectory (file-name-sans-extension (buffer-file-name)))))`_H}
#define $1

$0

#endif // $1
